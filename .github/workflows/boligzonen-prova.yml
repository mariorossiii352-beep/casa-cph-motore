name: boligzonen-prova

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  prova:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Browser
        run: |
          pip install playwright==1.62.0
          python -m playwright install --with-deps chromium
      - name: Prova
        working-directory: siti
        run: python prova_boligzonen.py
